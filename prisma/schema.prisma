datasource db {
    provider = "postgresql"
    url      = "postgres://avnadmin:AVNS_aMtLgxH51pPj0Dn9CYg@pg-9eb48e7-dev-41b5.a.aivencloud.com:16488/defaultdb?sslmode=require"
}

generator client {
    provider = "prisma-client-js"
    // output   = "./generated/prisma-client-js"
}

model User {
    id      Int      @id @default(autoincrement())
    email   String   @unique
    name    String
    createdAt DateTime @default(now())
}